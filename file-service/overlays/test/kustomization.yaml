apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

# No images: entry yet — this service has never been built. jarvis-
# file-service's own Jenkinsfile adds it here on its first run (`kustomize
# edit set image` from this directory), same as conversation-service's.
# Until then, ArgoCD deploys the base's literal `jarvis-file-service:local`
# tag, which will sit in ImagePullBackOff — expected for a brand-new service
# before its first pipeline run.

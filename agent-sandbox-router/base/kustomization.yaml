apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml

# No images: transformer here — set per environment in each overlay, same
# pattern as sandbox/base and agentsandbox/base.

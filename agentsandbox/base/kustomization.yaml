apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- sandboxtemplate.yaml
- warmpool.yaml
- networkpolicy.yaml

# No images: transformer here — set per environment in each overlay, same
# pattern as sandbox/base. Kustomize rewrites the SandboxTemplate's
# container image; SandboxWarmPool references the Template by name only.

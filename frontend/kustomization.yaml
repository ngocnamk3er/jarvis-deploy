apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- frontend.yaml

# Rewritten by jarvis-frontend's own Jenkinsfile "Bump manifest" stage
# after each image push — see backend/kustomization.yaml for the same
# pattern.
images:
- name: jarvis-frontend
  newName: 192.168.1.61:5050/root/jarvis-frontend
  newTag: 747aba4c

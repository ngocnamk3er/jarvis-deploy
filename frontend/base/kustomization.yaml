apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- frontend.yaml

# No images: transformer here — set per environment in each overlay
# instead (see backend/base/kustomization.yaml for the same reasoning).
